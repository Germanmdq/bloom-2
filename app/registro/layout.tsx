import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Club Bloom | Registrate gratis",
  description: "Sumate al programa de lealtad: café gratis, beneficios y descuentos exclusivos.",
};

export default function RegistroLayout({ children }: { children: React.ReactNode }) {
  return children;
}
